# Open-port-Scanner-for-single-host-network
A Python CLI tool that scans a single host or an entire /24 network for open TCP ports. Identifies services, detects protocols via banner grabbing and signature matching, and auto-generates a timestamped report. Built with Python's standard library — no external dependencies required.
